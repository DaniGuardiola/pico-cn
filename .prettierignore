# build output and deps
node_modules/
dist/
pnpm-lock.yaml

# generated artifacts — the freshness CI job byte-compares these against
# compiler output, so they must never be reformatted
*.generated.ts

# written by changesets during release PRs, without prettier
CHANGELOG.md

# vendored research copies and private planning
.research/
.plans/
.claude/
.deepsec/
